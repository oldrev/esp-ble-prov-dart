name: esp_ble_prov_dart_example
description: Example app for testing ESP BLE provisioning.
publish_to: none

version: 0.1.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  esp_ble_prov_dart:
    path: ..
  cupertino_icons: ^1.0.8
  universal_ble: 1.2.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
